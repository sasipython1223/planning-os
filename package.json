{
  "name": "@planner/root",
  "version": "1.0.0",
  "private": true,
  "description": "Planner-Studio monorepo root",
  "scripts": {
    "build:engine": "pnpm --filter @planner/engine run build",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "dev": "pnpm run build:engine && pnpm -r --parallel run dev",
    "test": "pnpm -r run test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
